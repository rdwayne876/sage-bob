/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['s.gravatar.com', 'cdn.auth0.com'], // Add other domains as needed
      },
};

export default nextConfig;
